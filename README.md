# glab-preset
> Gitlab preset tool.

## installation
```shell
pip install glab-preset -U
```

## usage
```shell
$ glab-preset --help
Usage: glab-preset [OPTIONS]

Options:
  --type [react_oss]
  --help              Show this message and exit.
```
